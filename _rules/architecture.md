# Architecture

Project-to-any currently has three active layers:

1. Component intent registry
2. SuperCSS visual contract
3. Angular component expo test bench

Targets translate contracts. Targets do not invent UI.

Full app generation returns only after the component registry and SuperCSS contracts are stable.

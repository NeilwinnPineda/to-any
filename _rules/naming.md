# Naming Conventions

> Source: `_rules/ruleset.md`  
> Status: placeholder — conventions not yet defined in v0.1

---

Naming conventions for this system are pending definition.

This file exists to reserve the space and signal that naming is a first-class concern.

Expected coverage when defined:

- folder naming (template folders, theme folders, connector folders)
- file naming (scripts, config files, generated output)
- export naming (TypeScript exports from the content generator)
- CSS class naming (supercss class patterns)
- connector naming (contract file names)
- generated project naming (output directory conventions)

---

_Update this file when naming patterns are established. Reference `_rules/ruleset.md` Rule 4 — shared conventions should not be duplicated across templates._
